class Blogers{
    constructor(id, username, email, password, profilePict){
            this.id = id;
            this.username = username;
            this.email = email;
            this.password = password;
            this.profilePict = profilePict;
    }
}

module.exports = Blogers;